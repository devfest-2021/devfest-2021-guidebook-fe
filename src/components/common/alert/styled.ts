import styled from 'styled-components';

export const StyledAlert = styled.div`
  position: fixed;
  top: 100px;
  right: 0px;
  display: flex;
  justify-content: flex-end;
  align-items: center;
  z-index: 1;
`;
